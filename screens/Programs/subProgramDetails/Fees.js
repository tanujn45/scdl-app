import React from "react";
import { StyleSheet, View, Text } from "react-native";

const Fees = (props) => {
  return (
    <View>
      <Text>Fees</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Fees;
