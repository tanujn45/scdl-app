import React from "react";
import { StyleSheet, View, Text } from "react-native";

const ProgramStructure = (props) => {
  return (
    <View>
      <Text>ProgramStructure</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ProgramStructure;
